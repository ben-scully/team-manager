<%= form_with(model: team_event, local: true) do |form| %>
  <% if team_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_event.errors.count, "error") %> prohibited this team_event from being saved:</h2>

      <ul>
      <% team_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team %>
    <%= form.text_field :team, id: :team_event_team %>
  </div>

  <div class="field">
    <%= form.label :event %>
    <%= form.text_field :event, id: :team_event_event %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
