<%= form_with(model: team_user_role, local: true) do |form| %>
  <% if team_user_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_user_role.errors.count, "error") %> prohibited this team_user_role from being saved:</h2>

      <ul>
      <% team_user_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team %><br>
    <%= form.collection_select :team_id, Team.all, :id, :display_name %>
  </div>

  <div class="field">
    <%= form.label :user %><br>
    <%= form.collection_select :user_id, User.all, :id, :display_name %>
  </div>

  <div class="field">
    <%= form.label :team_role %><br>
    <%= form.collection_select :team_role_id, TeamRole.all, :id, :display_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
